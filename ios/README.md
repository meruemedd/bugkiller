# iOS 骨架

源码入口：`BugKiller/BugKillerConfig.swift`。

推荐：

1. Xcode → Create a new Xcode project（App）→ Product Name `BugKiller`。
2. 将 `BugKillerConfig.swift` 加入工程；开发期在 Info 中允许本地 HTTP（App Transport Security 例外）。
3. Run 到模拟器，联调 `http://127.0.0.1:8787`。

详见根目录 `README.md` 与 `scripts/mobile-hint.sh`。
